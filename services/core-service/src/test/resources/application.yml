spring:
  jpa:
    hibernate:
      ddl-auto: validate
  session:
    store-type: redis
    redis:
      repository-type: indexed

app:
  auth:
    redis:
      key-secret: test-secret
  storage:
    type: local
    local:
      root: ${java.io.tmpdir}/coactivity-test-storage
